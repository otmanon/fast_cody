[tool.cibuildwheel]
build-verbosity = "3"
[build-system]
requires = [
    "packaging",
    "numpy",
    "scipy",
    "polyscope",
    "cvxopt",
    "libigl",
    "scikit-learn",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16"
]

build-backend = "setuptools.build_meta"